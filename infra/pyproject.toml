[project]
name = "infra"
version = "0.1.0"
description = "AWS CDK stacks for the greghlewis.com portfolio account"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-lib>=2.180.0",
    "constructs>=10.0.0,<11.0.0",
]
