# Raw upstream responses (the Sleeper players file alone is 14 MB) and the
# parquet intermediates. Reproducible by re-running the fetchers.
data/
.venv/
# Server-render output from results/render.cjs; regenerated by every gate run.
results/out/
