"""Feature engineering for player profiles.

`catalog.py` is the canonical inventory of features we plan to compute.
Per-feature implementations live in position-specific modules and are
registered against the catalog as they ship.
"""
